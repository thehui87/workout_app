import 'pdf_service_mobile.dart' if (dart.library.html) 'pdf_service_web.dart';

// You can now call `someFunction` and it will resolve to the correct implementation
void someFunction() {
  // This will call the appropriate version based on the platform
  someFunction();
}
